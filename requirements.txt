# Runtime dependencies for running this project from PyCharm or the command line.
# These packages are inferred from the current code paths in run.py,
# BottomUpAgent/group_a/*, BottomUpAgent/group_b/*, and base_model/*.

# Model / HTTP layer
requests>=2.31

# Config loading (.env is optional in code, but supported by run.py)
python-dotenv>=1.0
PyYAML>=6.0

# Vision / screenshot / image processing
numpy>=1.26
Pillow>=10.0
mss>=9.0
opencv-python>=4.8

# Desktop automation / window discovery
PyAutoGUI>=0.9.54
PyGetWindow>=0.0.9

# Windows-specific helpers for real game automation
pywin32>=306; platform_system == "Windows"